package syntax

/**
 * Created by Administrator on 2021/6/16.
 */
fun main() {
//    val a = arrayOf(1,2)
//    a.forEach { println("it = ${it}") }
//
//    val b = arrayOfNulls<String>(3)
//    b.forEach { println("it = ${it}") }
//
//    val c = IntArray(3){ it }
//    c.forEach { println(it) }


//    val integers = intArrayOf(1, 2, 3, 4)
//    integers.forEach { println("it = ${it}") }
//    for (i in integers.indices) {
//        integers[i] = i * i
//    }

//    integers.forEach { println("it = ${it}") }

//    println(" = ")

}